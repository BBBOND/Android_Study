<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kim.ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".textview.TextViewActivity" />
        <activity android:name=".textview.Activity1" />
        <activity android:name=".textview.Activity2" />
        <activity android:name=".edittext.EditTextActivity" />
        <activity android:name=".button.ButtonActivity" />
        <activity android:name=".cs.CheckboxSeekbarActivity" />
        <activity android:name=".imageview.ImageViewActivity" />
        <activity android:name=".dateandother.DateActivity" />
        <activity android:name=".gallery.GalleryActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
</manifest>
